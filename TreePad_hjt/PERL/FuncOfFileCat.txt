
-X
Синтаксис:
	-X [ДЕСКРИПТОР|ИМЯ_ФАЙЛА]
	-X
Описание:
	Обозначает большую группу операция для проверки параметров и состояний файлов, заданных либо дескриптором, либо именем. ...

chdir
Синтаксис:
	chdir [ВЫРАЖЕНИЕ]
Описание: 
	Изменение текущего рабочего каталога на каталог, определяемый значением параметра: ВЫРАЖЕНИЕ. Если параметр опущен, домашний каталог становится текущим. Возвращает значение «истина» в случае успешного выполнения операции замены текущего каталога и «ложь» в противном случае.

chmod
Синтаксис:
	chmod СПИСОК
Описание:
	Изменение режима доступа к файлам, имена которых представлены в списке параметров. Первый элемент списка является восьмеричным числом, определяющий новый режим доступа к файлам. Возвращает количество файлов, для которых операция изменения режима доступа завершилась успешно.

chown
Синтаксис:
	chown СПИСОК

Описание:
	Изменения владельца и группу владельца файлов, имена которых представлены в списке параметров. Первые два элемента списка являются числами, представляющими uid и gid. Возвращает количество файлов, для которых операция изменения владельца и группы окончилась успешно.

chroot
Синтаксис:
	chroot ИМЯ_КАТАЛОГА
	chroot
Описание:
	Определяет новый корневой каталог для относительных (начинающийся с косой черты – /) имён файлов, для процесса пользователя или для порождённых процессов. Не меняет текущий рабочий каталог. В отсутствие параметров используется значение специальной переменной $_. Может вызываться только суперпользователем.

close
Синтаксис: 
	close [ДЕСКРИПТОР]
	close
Описание:
	Закрывает файл либо программный канал, ассоциированный с дескриптором, заданным параметром ДЕСКРИПТОР. Возвращает булево значение «истина», если успешно очищен буфер и закрыт системный файловый дескриптор. Без параметра закрывает файл или программный канал, ассоциированный с текущим дескриптором, выбранным функцией select().

closedir
Синтаксис:
	closedir ДЕСКРИПТОР

Описание:
	Закрывает каталог, ассоциированный с дескриптором каталога, заданного параметром ДЕСКРИПТОР. Возвращает булево значение «истина», если каталог успешно закрыт.

fentl
Синтаксис:
	fentl ДЕСКРИПТОР, ФУНКЦИЯ, СКАЛЯР

Описание:


glob
Синтаксис:
	glob ВЫРАЖЕНИЕ

Описание:
	Возвращает найденные в текущем каталоге файлы, имена которых удовлетворяют заданному шаблону (с использованием метасимволов UNIX *, ?). Значением выражения должна быть строка, содержащая шаблон имён файлов.

ioctl
Синтаксис:
	ioctl ДЕСКРИПТОР, ФУНКЦИЯ, СКАЛЯР

Описание:


link
Синтаксис:
	link СТАРЫЙ, НОВЫЙ

Описание:
	Создаёт новый файл, связанный с файлом, определённым параметром СТАРЫЙ («жёсткая» ссылка). Возвращает булево значение «истина» в случае успешного создания файла и «ложь» в противном случае.

lstat
Синтаксис:
	lstat [ДЕСКРИПТОР|ВЫРАЖЕНИЕ]
	lstat
Описание:
	Возвращает список значений полей структуры индексного дескриптора символической ссылки на файл. Если параметр опущен, то используется значение специальной переменной $_.

mkdir
Синтаксис:
	mkdir КАТАЛОГ, РЕЖИМ

Описание:
	Создание нового каталога с именем, заданным параметром: КАТАЛОГ, и режимом доступа, определяемым параметром: РЕЖИМ. При успешном создании каталога возвращает булево значение «истина», в противном случае – «ложь» и в переменную $! заносится сообщение об ошибке.

open
Синтаксис:
	open ДЕСКРИПТОР, РЕЖИМ, ИМЯ_ФАЙЛА
	open ДЕСКРИПТОР[,ВЫРАЖЕНИЕ]
Описание:
	Первая форма открывает файл, имя которого является значением параметра: ИМЯ_ФАЙЛА, в режиме, заданным строковым значением параметра: РЕЖИМ, и связывает его с дескриптором, определённым параметром: ДЕСКРИПТОР. При использовании второй формы в случае присутствия параметра выражение оно должно вычисляться как строка, в которой определены и режим открытия файла, и его имя. Если во второй форме параметр:ВЫРАЖЕНИЕ не задан, то вместо него используется содержимое скалярной переменной с тем же именем, что и дескриптор: ($ДЕСКРИПТОР).

opendir
Синтаксис:
	opendir ДЕСКРИПТОР, ВЫРАЖЕНИЕ

Описание:
	Открывает каталог, имя которого равно значению параметра: ВЫРАЖЕНИЕ, и связывает его с дескриптором, определённым параметром: ДЕСКРИПТОР. Имена дескрипторов каталога хранятся в собственном пространстве имён Perl.

readlink
Синтаксис:
	readlink ВЫРАЖЕНИЕ
	readlink
Описание:
	Возвращает значение символической ссылки, определённой параметром: ВЫРАЖЕНИЕ, если символическая ссылка реализуется операционной системой; в противном случае выдаёт фатальную ошибку. Если при получении значения символической ссылки были получены системные ошибки, возвращает неопределённое значение и в специальную переменную: $! заносится сообщение об ошибке. Если параметр опущен, используется значение переменной: $_.

rename
Синтаксис:
	rename СТАРОЕ_ИМЯ, НОВОЕ_ИМЯ

Описание:
	Переименовывает файл. Возвращает 1 в случае успешного переименования и ноль в противном случае.

rmdir
Синтаксис:
	rmdir ИМЯ_КАТАЛОГА
	rmdir
Описание:
	Удаление каталога, заданного параметром: ИМЯ_КАТАЛОГА. Каталог удаляется, только если он пустой. Возвращает 1 в случае успешного удаления или ноль в противном случае, сохраняя в переменной $! сообщение об ошибке. Если параметр опущен, то содержимое специальной строки $_ используется как имя каталога.

stat
Синтаксис:
	stat [ДЕСКРИПТОР]
	stat [ВЫРАЖЕНИЕ]
Описание:
	Возвращает список значений тринадцати полей структуры индексного дескриптора файла, заданного либо своим дескриптором (параметр: ДЕСКРИПТОР), либо значением выражения (параметр: ВЫРАЖЕНИЕ). Если параметр опущен, то используется содержимое символьной переменной $_. В случае ошибки возвращает пустой список.

symlink
Синтаксис:
	symlink СТАРОЕ_ИМЯ, НОВОЕ_ИМЯ

Описание: создаёт символическую ссылку (параметр: НОВОЕ_ИМЯ) на существующий файл (параметр: СТАРОЕ ИМЯ). Возвращает 1 в случае успешного завершения и ноль в случае неудачи.

umask
Синтаксис:
	umask ВЫРАЖЕНИЕ

Описание:


unlink
Синтаксис:
	unlink СПИСОК

Описание:
	Удаляет файлы, чьи имена определены значениями элементов параметра: СПИСОК. Возвращает количество успешно удалённых файлов.

utime
Синтаксис:
	utime СПИСОК

Описание:
	Изменяет время доступа и последней модификации файлов, определённых в параметре: СПИСОК. Первые два элемента списка должны быть численными и представлять соответственно время последнего доступа и время последней модификацией файлов. Возвращает количество файлов, для которых успешно была выполнена указанная операция.

